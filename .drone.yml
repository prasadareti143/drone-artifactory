pipeline:
  build:
    image: node:carbon
    commands:
      - make install
      - make test
  docker:
    image: plugins/docker
    repo: prasadaws143/drone-artifactory
    tag: latest
    secrets: [prasadareti143 , Volley@143 ]
    when:
      event: push
      branch: master
      local: false

plugin:
  name: Artifactory
  desc: Publish files and artifacts to Artifactory
  type: publish
  image: athieriot/drone-artifactory
  labels:
    - artifactory
